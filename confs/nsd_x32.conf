[una]
components = linux-image busybox dropbear mxmux wireguard-tools nftables nsd
kernel_name = nsd_to_x32.efi
etc_dir = confs/etcs/shell
kconfig = confs/kerns/
cpu_flags = -O3 -march=x86-64-v4 -pipe
